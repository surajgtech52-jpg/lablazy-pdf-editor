const AppConfig = {
    // Set to true to connect to your local signaling server (localhost:8080) for testing.
    // Set to false to connect to the production Render host.
    FORCE_LOCAL: false,

    // Production WebSocket signaling server host
    SIGNALING_HOST: 'lablazy-signaling-server.onrender.com'
};
